# Firebase Messaging (added for push notifications) transitively pulls in
# AndroidX dependencies (androidx.core, androidx.fragment, androidx.lifecycle,
# etc. via play-services-base). AGP refuses to build with AndroidX
# dependencies on the classpath unless this is explicitly enabled.
android.useAndroidX=true
